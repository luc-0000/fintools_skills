# Agent Client Skill PRD

## 1. 目标

把 `agent-client-template` 封装成一个可直接被 Agent 系统调用的 Skill。

## 重要提示

- `fintools-agent-client-skill/agents_client/` 是受保护的上游客户端代码目录，后续调整默认不允许修改这里的文件
- 如需修复运行流程、日志、产物落盘、目录结构或宿主集成行为，只能优先在 `scripts/`、`docs/`、`tests/` 等包装层处理
- 只有在需求明确要求并得到额外确认时，才允许评估是否修改 `agents_client/`；默认一律视为禁改区

这个 Skill 要解决三件事：

- 调用远端 Deep Research Agent 或 Trading Agent
- 在终端持续输出执行过程和中间结果，避免黑盒等待
- 产出 ZIP 报告，并把结果导出到可持久保存的位置

## 2. 默认运行假设

- 默认运行方式是在本地创建临时工作目录，不依赖沙箱
- 不假设存在预先准备好的本地工作目录
- Python、虚拟环境、依赖、缓存、下载文件默认都在当前工作目录内
- 所有需要交付给用户的结果，必须显式导出到持久化位置，不能只留在临时工作目录
- 如果本地默认 Python 版本不满足要求，必须先尝试发现本机已安装的 `python3.10+`
- 只有在本机不存在可用 `python3.10+` 时，才回退到本地 `conda create` 新环境执行
- 无论使用临时工作目录还是本地 conda 兜底环境，任务结束后都要按清理规则处理
- Skill 支持可选 `work_dir` 参数
- `work_dir` 表示该 skill 的主目录，而不是单次运行目录
- 如果用户未提供 `work_dir`，优先使用默认主目录 `/tmp/fintools-agent-client-skill-runs/`
- 如果 `/tmp` 不可用，再回退到系统临时目录
- 主目录下应包含共享环境目录和单次 run 目录
- 共享环境目录固定为主目录下的 `shared-envs/`
- 如果需要流式输出诊断或探针，其输出目录固定为主目录下的 `probe/`
- 每次执行都必须在主目录下创建独立的 run 子目录，例如 `run-<agent_type>-<stock_code>-<mode>-<timestamp>`
- run 子目录名应尽量反映本次任务信息，至少包括：Agent 类型、股票代码、运行方式、执行时间
- 启动后必须立即告诉用户主目录和本次 run 目录
- 用户只需要理解一个目录概念：`work_dir`
- 运行环境、日志、ZIP 报告、结果摘要都应放在本次 run 子目录下
- Python 运行环境应尽量共享，避免每次 run 重复安装依赖
- Skill 必须是完全独立的目录，不依赖 `agent-client-template` 仓库中的其他文件
- `agents_client/` 和 `requirements.txt` 等运行所需文件必须随 skill 一起打包
- 即使原始 `agent-client-template` 目录被删除，skill 仍必须可以运行
- 执行日志必须写入当前 run 子目录下的 `run.log`
- `FINTOOLS_ACCESS_TOKEN` 应允许保存在主目录下，避免重复输入
- 只要主目录未删除，已保存的 token 和已就绪的共享环境都应可复用

## 3. 输入

必填：

- `FINTOOLS_ACCESS_TOKEN`
- Agent 类型：`deep_research` 或 `trading`
- 运行方式：`streaming` 或 `polling`
- 股票代码
- Agent URL

可选：

- `work_dir`

输入校验要求：

- 缺少 `FINTOOLS_ACCESS_TOKEN` 时，必须立即报错并停止执行
- 缺少 Agent 类型时，必须立即报错并停止执行
- 缺少运行方式时，必须立即报错并停止执行
- 缺少股票代码时，必须立即报错并停止执行
- 缺少 Agent URL 时，必须立即报错并停止执行
- 不允许使用代码内默认值作为隐式兜底

运行方式命名要求：

- 面向用户时，不直接使用 `db polling` 作为主名称
- `streaming` 作为对外参数名，用户说明使用“实时模式”
- `polling` 对外名称使用“轮询模式”
- 如需展示技术说明，可在文档中补充：
  - 实时模式：服务持续推送中间结果
  - 轮询模式：不是一直保持连接，而是隔一段时间查一次任务进度，适合长时间任务

## 4. 执行流程

1. 确定工作目录：
   - 用户提供 `work_dir` 时，将其作为主目录
   - 用户未提供时，优先使用默认主目录 `/tmp/fintools-agent-client-skill-runs/`
   - 如果 `/tmp` 不可用，再回退到系统临时目录
2. 在主目录下创建本次独立 run 子目录，例如 `run-<agent_type>-<stock_code>-<mode>-<timestamp>`
   - 如果同一秒内目录名冲突，可在末尾追加顺序号，例如 `-002`
3. 启动时打印主目录和本次 run 目录，以及这些目录是“用户指定”还是“自动创建”
4. 优先使用当前 run 目录中的 Python 环境执行：
   - 先检查当前默认 `python` 或 `python3` 是否满足 Python 3.10+
   - 如果不满足，自动探测本机已安装的 `python3.10`、`python3.11`、`python3.12` 等可用解释器
   - 若找到可用解释器，则优先复用主目录下的共享 `venv`
   - 若没有可用解释器，则回退到主目录下的共享 `conda-env`
   - 共享环境应按 Python 版本和 `requirements.txt` 指纹区分，避免版本污染
   - 共享环境命名示例：
     - `shared-envs/venv-py311-<requirements_hash>/`
     - `shared-envs/conda-py310-<requirements_hash>/`
   - 多个并发任务共用共享环境时，必须通过锁文件机制避免并发初始化冲突
   - 如果共享环境目录下已经存在 `.ready` 标记文件，则视为环境已就绪，后续任务直接复用，不再重复安装依赖
5. 如果既没有可用 `python3.10+`，也无法创建共享 `conda-env`，必须立即报错并停止执行
6. 如果既没有可用 `python3.10+`，也没有可用 conda，必须立即报错并停止执行
7. 检查 `FINTOOLS_ACCESS_TOKEN`
   - 用户首次提供 token 后，应将其保存到主目录下的本地配置文件
   - 后续运行优先读取主目录中已保存的 token
   - 只有主目录中不存在 token 时，才要求用户重新提供
8. 启动时检查 skill 自身目录中是否已经包含运行所需文件，至少包括：
   - `agents_client/`
   - `requirements.txt`
9. 如果 skill 自身目录不完整，必须立即报错并停止执行
10. 根据用户选择运行 Deep Research 或 Trading
11. 根据用户选择的运行方式执行：
   - 实时模式：持续接收并打印中间事件
   - 轮询模式：创建任务后定时查询状态，直到任务完成
12. 持续打印状态、事件、中间结果、错误和完成信息
   - 必须使用无缓冲输出，确保中间结果实时可见
   - 必要时使用 `python -u`、显式 `flush=True` 或等效方式避免输出缓冲
   - 终端输出必须同时写入当前 run 子目录下的 `run.log`
13. 任务完成后下载 ZIP 报告到当前 run 目录
14. 在当前 run 目录写入结果摘要
15. 输出最终摘要：是否成功、使用了哪种运行环境、共享环境目录、主目录路径、本次 run 目录、运行方式、处理了多少事件、报告是否下载成功
16. 清理规则：
   - `--cleanup` 仅删除当前 run 子目录
   - 不删除主目录
17. 如果执行流式输出探针或诊断脚本：
   - 也必须使用同一个 `work_dir` 主目录
   - 探针输出统一落到主目录下的 `probe/`
   - 不允许再创建诸如 `fintools-test` 之类与 skill 名称无关的散落目录

## 5. 输出

Skill 执行后必须提供：

- 终端输出：连接中、执行中、中间结果、完成、失败等状态
- 当前 run 目录中的 ZIP 报告、日志和运行产物
- 当前 run 目录中的结果摘要
- 结果摘要：成功/失败、使用的运行环境、共享环境目录、主目录、本次 run 目录、运行方式、事件数、报告结果

必须明确区分：

- 主目录
- 共享环境目录
- `probe/` 目录（如果使用诊断脚本）
- 本次 run 子目录

## 6. 验收标准

以下条件全部满足，视为交付合格：

- 只要求用户提供 `FINTOOLS_ACCESS_TOKEN`、Agent 类型、股票代码和 Agent URL 等基础运行参数
- 用户可以在“实时模式”和“轮询模式”之间明确选择
- 默认会在临时工作目录中执行
- 当前默认 Python 不满足要求时，会先自动探测本机可用的 `python3.10+`
- 本机没有可用 `python3.10+` 时，才会回退到本地 `conda create` 新环境运行
- 如果既没有可用 `python3.10+`，也没有 conda，会立即报错并停止执行
- 支持可选 `work_dir`
- 未提供 `work_dir` 时，会自动创建默认主目录，并在其中创建 run 子目录
- 并发任务会落在同一个主目录下的不同 run 子目录，不互相覆盖
- 主目录下的共享 `venv` 和共享 `conda-env` 可被多个任务复用
- 如果使用流式输出探针，其输出也会位于同一个主目录下的 `probe/`
- 共享环境会按 Python 版本和 `requirements.txt` 指纹进行隔离
- 共享环境初始化采用锁文件机制，避免并发创建冲突
- 共享环境存在 `.ready` 标记时，不会在每次运行时重复安装依赖
- 主目录中已保存的 `FINTOOLS_ACCESS_TOKEN` 可被后续任务复用，无需每次重新输入
- Skill 复制到其他目录后，仍能直接运行，不依赖外部仓库目录结构
- 即使原始 `agent-client-template` 目录被删除，skill 目录本身仍包含全部运行必需文件
- ZIP 报告、summary 和运行产物都位于同一个 run 子目录下，不分散到多个目录
- 当前 run 子目录下会生成 `run.log`，并且其内容与终端输出保持同步
- Deep Research 和 Trading 两条主路径都能跑通
- 执行过程中终端持续有输出，不能长时间无反馈
- OpenClaw 或其他 Agent 宿主执行时，中间结果必须实时打印到终端，不能只在最后汇总输出
- 中间结果输出必须采用无缓冲方式，不能因标准输出缓冲导致延迟显示
- 缺少股票代码或 Agent URL 时会立即报错，不会继续执行
- 缺少 token、下载失败、导出失败时有明确提示
- 任务完成后能在当前工作目录中生成 ZIP 报告
- 任务完成后能在当前 run 子目录中生成 ZIP 报告
- 任务完成后能在当前 run 子目录中生成结果摘要
- 默认不会自动删除本次 run 子目录
- 只有用户明确要求清理时才删除本次 run 子目录
- `--cleanup` 只删除当前 run 子目录，不删除主目录
- 文档明确说明：依赖装在哪里、临时目录是否会清理、哪些结果会丢失、哪些结果会保留

## 7. 非目标

- 不新增 Agent 业务逻辑
- 不要求 Docker 作为默认运行方式
- 不依赖宿主机全局 Python 环境
- 不把“轮询模式”作为默认主路径，只作为可选补充能力
- 不复用历史遗留 conda 环境，兜底时必须新建并在结束后清理
- 不使用与具体宿主产品强绑定的临时目录命名

## 8. 交付物

- `SKILL.md`
- `examples/`
- `tests/`
- `index_entry.json`
- 可选：`CHANGELOG.md`
